apiVersion: v1
kind: Service
metadata:
  name: {{ template "igcb-rlo-ui.fullname" . }}
  labels:
    app: {{ template "igcb-rlo-ui.name" . }}
    chart: {{ template "igcb-rlo-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ template "igcb-rlo-ui.name" . }}
    release: {{ .Release.Name }}
