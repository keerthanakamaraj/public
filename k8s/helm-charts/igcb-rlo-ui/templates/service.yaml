apiVersion: v1
kind: Service
metadata:
  name: {{ template "igcb-quantum-ui.fullname" . }}
  labels:
    app: {{ template "igcb-quantum-ui.name" . }}
    chart: {{ template "igcb-quantum-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ template "igcb-quantum-ui.name" . }}
    release: {{ .Release.Name }}
